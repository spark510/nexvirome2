# Chimera definition

A contig is a candidate chimera when non-overlapping segments map with strong support to
different source genomes and a breakpoint is supported by the known simulation truth or by
an independently validated competing alignment. A different best hit alone is insufficient:
recombination, incomplete references, conserved domains, and strain variation can create the
same pattern.

